/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the old i18n config as it's not supported in App Router
  // We'll handle i18n through the app directory structure
}

export default nextConfig
